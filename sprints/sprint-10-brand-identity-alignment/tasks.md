# Sprint 10 — Tasks

> Before implementing any Sprint 10 work, change the matching task row to `In Progress`.

| ID | Task | Purpose / Scope | Acceptance Criteria | Owner | Status | Notes |
|---|---|---|---|---|---|---|
| S10-T01 | Source canonical Inspire2Live brand assets | Replace inference-from-one-PNG with the organisation's actual logo files, palette, and type specimens — `inspire2live.org` is unreachable from this environment. | Repo contains source-quality logo files (SVG preferred, plus PNG fallbacks, light- and dark-background variants) under `public/brand/`, plus a documented palette (hex values) and typeface reference sourced from the organisation directly or its live site. | Codex | In Progress | The canonical `inspire2live` lock-up (wordmark + dotted mark) was supplied directly and committed as `public/brand/inspire2live-logo.png` — this is now the source of truth for T02–T05, not an inference. A second, transparent-background export of the same lock-up was supplied and swapped in (409×262, alpha channel verified programmatically: ~74% of pixels fully transparent), replacing the earlier opaque-white-background raster — this removes the "white box on non-white surfaces" problem. **Important caveat discovered during the swap:** the wordmark text renders in dark charcoal (`#2A2C35`), so on a dark/brand-charcoal background it becomes nearly illegible — only the red "2" and the colourful mark remain visible (verified by compositing onto `--color-secondary-500`). The transparency alone does **not** satisfy the "dark-background-safe variant" criterion; that still needs an organisation-supplied lock-up with a light/white wordmark for use on dark surfaces. Still outstanding: a vector (SVG) version, a true light-wordmark dark-background variant, a compact mark-only crop (the mark and wordmark overlap in the source raster, so a clean automated crop isn't possible), and typeface specimens. `inspire2live.org` / Brandfetch remain `host_not_allowed` from this environment. |
| S10-T02 | Replace placeholder colour tokens with the real brand palette | Translate the canonical brand colours into the existing token structure instead of the current orange/blue placeholder. | `--color-primary-*`, `--color-secondary-*`, and the neutral scale in `src/app/globals.css` are derived from the canonical assets; resulting hex values are documented in `docs/IMPLEMENTATION_GUIDE.md` §7; WCAG AA contrast is verified for every text-on-background and button combination that changes. | Codex | Completed | Sampled the committed logo programmatically: wordmark text → `#2A2C35` (now `--color-secondary-500`), the "2" / accent dot → `#D74247` (now `--color-primary-500`). Replaced the orange/blue placeholder scales in `src/app/globals.css` with red/charcoal scales built around those values, and shifted `--color-danger` from `#D63B3B` to `#B3261E` so it no longer sits visually on top of the new brand red (guardrail in `description.md`). Documented in `docs/IMPLEMENTATION_GUIDE.md` §7. Neutral scale left as-is — it already carries the same dark slate/navy undertone as the brand charcoal. **Still open:** the ~514 `orange-*` / `blue-*` Tailwind utility-class usages across ~71 component files reference Tailwind's stock palette directly (not these CSS tokens) and were intentionally left untouched per the "token substitution, not a redesign" guardrail — propagating the brand palette into those usages is a follow-on sweep (see notes on T07). |
| S10-T03 | Add the Inspire2Live logo to the app shell | Replace the current text-only header treatment with the real wordmark + mark. | Logo renders in the top navigation (desktop and mobile drawer), auth/login screens, and `src/app/layout.tsx` metadata (favicon / OG image); assets live under `public/brand/`; `next/image` usage, sizing, and alt text follow `docs/IMPLEMENTATION_GUIDE.md` conventions. | Codex | In Progress | Replaced the placeholder "I2L" badge + "Inspire2Live Platform" text with `next/image` renders of `public/brand/inspire2live-logo.png` (`alt="Inspire2Live"`) in `top-nav.tsx`, `login/page.tsx`, `reset-password/page.tsx`, and the marketing `app/page.tsx` header. **Still open:** `layout.tsx` favicon / OG image — the source lock-up doesn't crop cleanly to an icon-only mark (see T01), so a square icon variant from the organisation is needed before the favicon can be replaced. |
| S10-T04 | Align typography tokens with the brand typeface | Replace the placeholder Inter/Geist assumption with the brand's actual type system (or nearest licensed equivalent). | `--font-sans` (and any heading-specific tokens introduced) reflect the brand typeface; `docs/IMPLEMENTATION_GUIDE.md` §7 Typography is updated to match; headings and body copy render correctly across the dashboard, comms workspace, and emails. | TBD | Not Started | Blocked on T01 — the logo raster doesn't reveal a body/heading typeface; needs type specimens from the organisation's brand kit or live site (currently unreachable). Confirm licensing before adopting any non-system font. |
| S10-T05 | Apply brand assets to transactional email templates | Keep invitation and notification emails on-brand, not on the placeholder palette. | `src/lib/invitation-email.ts` (and any other email templates) reference the new logo asset and updated colour tokens. | TBD | Not Started | Email clients have limited CSS support — use inline styles and a hosted/CID logo image, consistent with the conventions in `docs/ENVIRONMENT_REFERENCE.md`. |
| S10-T06 | Update design-system documentation and traceability | Keep `docs/IMPLEMENTATION_GUIDE.md` and `docs/TRACEABILITY.md` authoritative once the swap lands. | `docs/TRACEABILITY.md` REQ-DS-001 status reflects the finalised, brand-accurate token set (no longer "partial / placeholder"); `docs/IMPLEMENTATION_GUIDE.md` §7 lists the real brand colour and typography values with source references. | Codex | In Progress | `docs/IMPLEMENTATION_GUIDE.md` §7 and `docs/TRACEABILITY.md` REQ-DS-001 updated to reflect the new brand-derived colour tokens and their source (`public/brand/inspire2live-logo.png`). Typography section still carries the Inter/Geist placeholder — closes once T04 lands. |
| S10-T07 | Visual regression smoke pass | Confirm the rebrand doesn't break legibility or functional colour cues anywhere in the app. | Documented manual or E2E pass across navigation, dashboard, auth flow, comms workspace, and email rendering; explicit confirmation that RAG status colours remain clearly distinguishable from the new brand red (REQ-A11Y-003 — icon + text, not colour alone); no WCAG AA contrast regressions. | TBD | Not Started | Ran the existing unit + E2E auth suites against the logo swap (updated `auth.spec.ts` to assert on the logo image instead of the removed "Inspire2Live Platform" text). A full before/after visual pass — including the still-untouched `orange-*`/`blue-*` component utility classes from T02 — remains for human QC before sign-off. |
